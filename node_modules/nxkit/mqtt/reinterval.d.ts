export declare class ReInterval<A extends any[]> {
    private _args;
    private _callback;
    private _interval;
    private _intervalid;
    constructor(callback: (...args: A) => void, interval: number, args: A);
    reschedule(interval: number): void;
    clear(): void;
    destroy(): void;
}
export default function reInterval<A extends any[]>(cb: (...args: A) => void, interval: number, ...args: A): ReInterval<A>;
