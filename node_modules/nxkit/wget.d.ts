export interface Options {
    renewal?: boolean;
    limit?: number;
    onProgress?(opts: {
        total: number;
        size: number;
        speed: number;
    }): void;
    timeout?: number;
}
export interface Result {
    total: number;
    size: number;
}
export interface WgetResult extends Promise<Result> {
    abort(): void;
}
interface Wget {
    (www: string, save: string, options?: Options): WgetResult;
    LIMIT: number;
}
declare const wget: Wget;
export default wget;
