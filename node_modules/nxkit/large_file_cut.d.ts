export declare function cut(filename: string, count: number): void;
export interface MergeOptions {
    target?: string;
    removeSource?: boolean;
}
export declare function merge(filename: string, options?: MergeOptions): number;
